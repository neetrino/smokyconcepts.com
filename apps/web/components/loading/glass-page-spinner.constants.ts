/** Shared size utility classes for the glass page spinner (Tailwind-scannable). */
export const GLASS_PAGE_SPINNER_SIZE_CLASS = 'h-[5.5rem] w-[5.5rem]';

/** Inset utility for the frosted glass disc inside the spinner ring. */
export const GLASS_PAGE_SPINNER_INSET_CLASS = 'inset-[0.7rem]';

/** Brand mark inside the spinner (square mark fits the circular disc). */
export const GLASS_PAGE_SPINNER_LOGO_SRC = '/assets/home/Asset%202@4x-8.webp';

/** Logo size inside the frosted glass disc. */
export const GLASS_PAGE_SPINNER_LOGO_CLASS = 'h-8 w-8 object-contain';
