/** Panel slide transition — keep in sync with `sizeModalPanelClass` */
export const SIZE_MODAL_PANEL_TRANSITION_MS = 480;

/** Backdrop fade — keep in sync with `sizeModalBackdropClass` */
export const SIZE_MODAL_BACKDROP_TRANSITION_MS = 380;

/** Unmount after the longest close transition finishes */
export const SIZE_MODAL_EXIT_DURATION_MS = SIZE_MODAL_PANEL_TRANSITION_MS;
