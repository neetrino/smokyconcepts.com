/** Below Tailwind `lg` (1024px): mobile + tablet gallery slide animations. */
export const MOBILE_GALLERY_MEDIA_QUERY = '(max-width: 1023px)';

/** Hero + thumbnail strip slide duration (ms) — keep in sync. */
export const GALLERY_SLIDE_DURATION_MS = 300;

/** Minimum horizontal swipe distance (px) to change the hero image on mobile. */
export const GALLERY_SWIPE_THRESHOLD_PX = 48;
