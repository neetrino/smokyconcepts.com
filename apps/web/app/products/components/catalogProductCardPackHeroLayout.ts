/** White card top padding — room for heroes overlapping upward (compact / catalog). */
export const PRODUCT_CARD_PACK_HERO_CARD_TOP_PADDING_CLASS_NAME =
  'max-sm:pt-[2.75rem] sm:pt-[3.5rem] lg:pt-[4rem]';

/** Pull image block up so packs sit over the card edge (default compact strip). */
export const PRODUCT_CARD_PACK_HERO_PULL_UP_CLASS_NAME = '-mt-[5.125rem] sm:-mt-[6.25rem]';

/** `/products` catalog strip — stronger mobile pull-up. */
export const CATALOG_PRODUCT_CARD_PACK_HERO_PULL_UP_CLASS_NAME =
  'max-sm:-mt-[7rem] sm:-mt-[5.75rem] lg:-mt-[6.25rem]';

/** Tuck title/dots under the hero baseline. */
export const PRODUCT_CARD_PACK_HERO_DETAILS_OFFSET_CLASS_NAME =
  '-mt-[2.75rem] sm:-mt-[3.25rem]';
